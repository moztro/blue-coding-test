@model UrlShortener.Models.UrlViewModel

@{
    ViewData["Title"] = "Success";
}

<h1>Success</h1>

<div>
    <h4>Here it is your shortened URL!</h4>
    <hr />
    <dl class="row">
        <dd class = "col-sm-10">
            @Html.ActionLink(Model.ShortenUrl, "Redirect", new { id = Model.Id } )
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Back to Shortener screen", "Create")
</div>
